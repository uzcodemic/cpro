// rules.cpp : 定义控制台应用程序的入口点。
//

#include "stdafx.h"


int _tmain(int argc, _TCHAR* argv[])
{
	int top,score;
	top = score = -(2+5) * 6 + (4+3 * (2+3));
	printf("top = %d \n",top);
	getchar();
	return 0;
}

